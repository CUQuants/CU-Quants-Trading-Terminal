[pytest]
testpaths = backend/tests
asyncio_mode = auto

markers =
    integration: hits a real exchange API (paper or live). Requires credentials, slow, network-dependent. Excluded by default; run with `pytest -m integration`.

# Run only unit tests by default; opt-in to integration tests explicitly.
addopts = -m "not integration"
