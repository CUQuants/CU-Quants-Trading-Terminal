# ---------------------------------------------------------------------------
# CU Quants Proxy — the terminal's only OKX credential
# ---------------------------------------------------------------------------
# The terminal holds no OKX keys. Every OKX call goes through the CU Quants
# Trading Gateway ("the Proxy"), which holds the club's vaulted OKX key.
# Ask the club liaison for an OPERATOR credential (trading-gateway/NEW-USER.md).

CUQ_PROXY_URL=https://your-gateway-host.example
CUQ_PROXY_API_KEY=cuq_op_...
CUQ_PROXY_SECRET=
CUQ_PROXY_OPERATOR_ID=cuq-000
CUQ_PROXY_OPERATOR_NAME=Your Name

# Optional: explicit wss:// base. Leave unset to derive it from CUQ_PROXY_URL
# (keeps the REST and WS bases from drifting apart).
# CUQ_PROXY_WS_URL=

# Simulated vs. live is NOT set here any more — it's a property of the
# gateway CUQ_PROXY_URL points at (OKX_SIMULATED on that gateway). To test
# against OKX's simulated environment, point at a gateway running in that
# mode.

# Optional: audit-reporting API (trading-gateway/proxy_admin.reporting).
# Reuses the CUQ_PROXY_* operator credential above — only the URL differs,
# since this is a separate service/port with no reverse-proxy route. Leave
# unset and the Reporting tab returns 503 instead of the app refusing to
# start.
# CUQ_REPORTING_URL=https://your-reporting-host.example

# ---------------------------------------------------------------------------
# Kraken — still called directly until its own migration
# ---------------------------------------------------------------------------
KRAKEN_API_KEY=
KRAKEN_API_SECRET=

# ---------------------------------------------------------------------------
# News Hub - optional provider credentials (all read-only)
# ---------------------------------------------------------------------------
# Missing keys disable only that provider; see GET /news/status.
FINNHUB_KEY=
CRYPTOPANIC_KEY=
# Must match the subscription: developer, growth, or enterprise.
CRYPTOPANIC_PLAN=growth
COINDESK_KEY=
MESSARI_KEY=
ALPACA_KEY=
ALPACA_SECRET=
MARKETAUX_KEY=
NEWSAPI_KEY=
# Install the optional Yahoo adapter with: uv sync --extra news
NEWS_YFINANCE_ENABLED=true
# Seconds between news refreshes (default 300; must be >= 30 and < 3600).
# Invalid values log a warning and fall back to 300.
NEWS_REFRESH_SECONDS=300
